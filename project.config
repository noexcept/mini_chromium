[access]
	inheritFrom = crashpad
[notify "crashpad-dev"]
	email = crashpad-dev@chromium.org
	type = all_comments
	type = submitted_changes
	header = cc
[label "Commit-Queue"]
[project]
	description = A scaled-back interface-compatible version of Chromium's base library
